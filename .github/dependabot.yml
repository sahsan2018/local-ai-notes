version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
  # Enable after Python project metadata is added.
  # - package-ecosystem: pip
  #   directory: /
  #   schedule:
  #     interval: weekly
